# Known Issues — Running List

A lightweight, ongoing log of bugs worth fixing, found either during the v1 code review or while doing v2 implementation work. This is a tracking list, not a design document — for the reasoning/detail behind an already-cataloged v1 finding, follow the link rather than duplicating it here.

Update this file whenever a phase turns up something that's real but out of scope for that phase, per `v2-roadmap.md`.

| Found | Area | Issue | Status |
|---|---|---|---|
| v2 §3a (2026-08-17) | `CentralServlet.doPost`, `ACTION_UPDATE` | Building a brand-new `Configuration` row for a never-before-seen `(worldName, islandName)` pair never sets `generatedDate` before `save()` — fails against the `generated_date NOT NULL` constraint. Never fired in practice because every world/island combo in existing seed data already has a row. Confirmed pre-existing (predates Phase 2) via `git show`. Fix: default to current date/time when not otherwise known. | Fixed (Phase 5, 2026-08-18) — `configuration.setGeneratedDate(new Date())` (and `setCreatedAt`) now set when the row is first built; verified end-to-end with a real Island run against a brand-new world/island pair, confirmed via DB. |
| v1 review | `World.java` constructor | `lowerCutStart = size - (lowerCut ^ 2)` uses bitwise XOR, not squaring — cripples the evolutionary algorithm's per-generation replacement rate to ~2% of intended. See `optimization-and-islands.md` §4. | Fixed (Phase 5, 2026-08-18) — changed to `size - (lowerCut * lowerCut)`. |
| v1 review | `Creature`'s clone constructor | Clones the `Missing` tree from the parent's `Twin` tree instead of its own `Missing` tree. See `optimization-and-islands.md` §4. | Fixed (Phase 5, 2026-08-18) — now clones `clone.patientCompare.getMissing()`. |
| v1 review | `Scorer.java` / `Configuration.yml` / `island.yml` | Four different places define the default 3×3 scoring-weight matrix, and they disagree with each other. See `optimization-and-islands.md` §3. | Fixed (Phase 6, 2026-08-18) — `Configuration.yml`'s `scoringWeights` block is now the one canonical source; `Scorer.java` reads its default from a loaded `Configuration` instead of hardcoding its own copy (and its javadoc no longer repeats a fourth disagreeing table), and `island.yml`'s block was corrected to match. |
| v1 review | `Scorer.registerMatch` | Test cases with expected status `Research`/`Not Sure` fall through into the "Should Not Match" scoring bucket instead of being excluded. See `optimization-and-islands.md` §2. | Open |
| v1 review | `RandomScriptServlet` | The "different"-pair column's pass/fail indicator appears to reuse the "same"-pair's result. See `trainer-pages.md`. | Open |
| v1 review | `DownloadHl7Servlet` | HL7 message-ID counter declared but never incremented; also queries the empty `patient` table. Low priority — table is being retired, not this servlet's logic. See `data-model.md` §5. | Resolved by removal (Phase 6, 2026-08-18) — `DownloadHl7Servlet` and the dead `patient` table it queried were both deleted, per confirmation from the maintainer that the table was never populated in production. |
| Prod deploy prep (2026-08-19) | `TestScriptExploreServlet` (`/TestScriptExploreServlet`, nav: Tools → "Explore Test Script") | Loads its `MIIS-*.txt` scripts via `getClass().getResourceAsStream(...)` — a classpath lookup. These files were moved out of the WAR into `legacy-test-data/` during the v2 rewrite (`v2-roadmap.md` §8) and this servlet's lookup was never repointed to match, unlike the sibling `MIIS-*` dropdown in `TestMatchingServlet`/`ReviewServlet`, which *was* removed per plan. Result: selecting any script NPEs. Confirmed broken today, in local dev too (files aren't on the classpath or in `target/` anywhere). `legacy-test-data/` is deliberately **not** part of the production deploy (see `production-deployment-plan.md`/checklist) since nothing functional currently depends on it being present. | Deferred — Nathan's plan: deploy to production and confirm nothing there actually needs `legacy-test-data/`, then rip out this servlet (and its nav entry) plus everything else tied to that folder (`TemplateDataBootstrap`, the folder itself) as dead weight. Don't remove until that production check happens. |

**Status values:** Open (not fixed, not scheduled) · Scheduled (tied to a specific roadmap phase) · Deferred (acknowledged, intentionally not fixing yet) · Fixed (note which commit/phase).
